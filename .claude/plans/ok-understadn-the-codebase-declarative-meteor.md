# Codebase Readability Refactor — MOVED

This plan was restructured into the coordinator + per-track layout (for parallel-chat execution). The canonical docs now live in:

- **`.claude/plans/codebase-readability/coordinator.md`** — shared status table, dependency model, decisions, verification (single source of truth)
- **`.claude/plans/codebase-readability/backend.md`** — `api/` + `open_notebook/` track
- **`.claude/plans/codebase-readability/frontend.md`** — `frontend/` track

Do not edit this file. Use the coordinator.
